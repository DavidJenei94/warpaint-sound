export interface SoundRecord {
  id: number;
  instrument: string;
  category: string;
  subCategory: string;
  description: string;
  coordinates: number[];
  imageUrl: string;
  soundUrl: string;
}
